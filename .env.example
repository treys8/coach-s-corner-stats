NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# Server-only — used by API routes that write derived tables (at_bats,
# game_live_state) which are RLS-locked to the service role.
SUPABASE_SERVICE_ROLE_KEY=""
