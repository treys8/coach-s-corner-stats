# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Next.js build output
.next
out
next-env.d.ts.bak
tsconfig.tsbuildinfo

*.local
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
tsconfig.tsbuildinfo

# Supabase CLI cache
supabase/.temp/
.vercel
.env*

# Dev-only no-login live-scoring demo (throwaway). The in-memory engine it
# uses (src/lib/scoring/demo-engine.ts) is committed because the scoring hooks
# import it, but the route entry point itself stays local.
/src/app/demo-scoring/

# Claude Code local workspace (worktrees, plans, settings)
.claude/
